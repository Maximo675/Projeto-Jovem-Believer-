# Web Framework
Flask==2.3.3
Flask-CORS==4.0.0
Flask-SocketIO==5.3.5
python-socketio==5.10.0
python-engineio==4.8.0
eventlet==0.35.1
gevent==23.9.1
gevent-websocket==0.10.1
Werkzeug==2.3.7

# Database ORM
SQLAlchemy==2.0.23
Flask-SQLAlchemy==3.1.1
psycopg2-binary==2.9.9
alembic==1.13.1

# Environment Variables
python-dotenv==1.0.0

# Authentication & Security
PyJWT==2.11.0
bcrypt==4.1.1
Flask-Login==0.6.3

# IA & Machine Learning
# openai desabilitado no Render: modo mock não usa o SDK e os proxy objects
# do openai>=1.x são incompatíveis com eventlet.monkey_patch() (crash no boot).
# Para reativar IA real: descomente a linha abaixo E configure OPENAI_API_KEY + USE_MOCK_AI=false
# openai==1.11.0
numpy==1.24.3
pandas==2.1.3

# API Validation
Marshmallow==3.20.1
pydantic==2.5.0

# Utility
requests==2.31.0
Pillow==10.1.0
pytesseract==0.3.10
# httpx pinnado: openai<=1.x passa `proxies` para httpx, removido no httpx>=0.28
# necessário enquanto openai estiver instalado (ex: cache do Render)
httpx==0.27.2

# WSGI Server
gunicorn==21.2.0
