# Web Framework
Flask==2.3.3
Flask-CORS==4.0.0
Flask-SocketIO==5.3.5
python-socketio==5.10.0
python-engineio==4.8.0
eventlet==0.35.1
gevent==23.9.1
gevent-websocket==0.10.1
Werkzeug==2.3.7

# Database ORM
SQLAlchemy==2.0.23
Flask-SQLAlchemy==3.1.1
psycopg2-binary==2.9.9
alembic==1.13.1

# Environment Variables
python-dotenv==1.0.0

# Authentication & Security
PyJWT==2.11.0
bcrypt==4.1.1
Flask-Login==0.6.3
msal==1.28.0
resend==2.10.0

# IA & Machine Learning
# openai instalado normalmente — o pacote em si não é o problema. O que
# quebra o boot no Render é o worker gunicorn rodando eventlet.monkey_patch()
# em cima dos proxy objects lazy do SDK. run.py bloqueia o import real da
# openai só nesse cenário (gunicorn); rodando local/Docker via "python run.py"
# o import é liberado normalmente. Ver comentário no topo de run.py.
# Versão atual da SDK (não precisa mais travar em 1.11.0: a partir da v2 a
# openai isolou sua própria dependência interna de HTTP, então não conflita
# mais com o resto do projeto — por isso o pin de httpx que existia aqui
# embaixo também não é mais necessário e foi removido).
openai==3.3.1
numpy==1.24.3
pandas==2.1.3

# API Validation
Marshmallow==3.20.1
pydantic==2.5.0

# Utility
requests==2.31.0
Pillow==10.1.0
pytesseract==0.3.10

# WSGI Server
gunicorn==21.2.0
